import { StyleSheet } from 'react-native';

import { TEXT_COLOR } from './contstants';

export default StyleSheet.create({
  text: {
    color: TEXT_COLOR,
  },
  headerText: {
    color: TEXT_COLOR,
  },
});
